# Sprint S7 — Detalhes do Anime End-to-End

> **Estado:** Em andamento — S7.1 concluída e aceita
> **Registro:** contemporâneo

## Objetivo

Transformar a rota de detalhes em uma funcionalidade completa baseada no anime
selecionado, reutilizando os contratos de domínio e a integração AniList já
existentes.

## Baseline

A rota já transportava `CatalogAnimeId`, preservava o contexto da pesquisa e
possuía deep link Web. `AnimeRepository` já oferecia a consulta de detalhes, mas
a tela era um placeholder e não executava o carregamento.

## Escopo e subtasks

| ID | Tarefa | Estado | Evidência |
|---|---|---|---|
| S7.1 | Estado e carregamento de detalhes | Concluída e aceita | `AnimeDetailsViewModel`, estados de UI, integração Koin, retry e testes |
| S7.2 | Estrutura visual principal | Planejada | capa, banner, títulos, sinopse e metadados essenciais |
| S7.3 | Conteúdo complementar | Planejada | gêneros, notas, datas, estúdios, trailer e relações justificadas |
| S7.4 | Navegação e integração | Planejada | integração final e navegação por relações quando aplicável |
| S7.5 | Validação end-to-end | Planejada | UI, temas, responsividade e regressão final |

## Critérios de aceite

- [x] O estado recebe a identidade obrigatória da rota.
- [x] O carregamento utiliza `AnimeRepository`, sem expor DTOs à UI.
- [x] Loading, falha e retry possuem comportamento implementado.
- [x] Retry não duplica chamadas fora do estado de falha.
- [ ] Os campos principais de `AnimeDetails` são apresentados.
- [ ] Campos ausentes e coleções vazias são adaptados visualmente.
- [ ] Temas claro e escuro e larguras aplicáveis são validados.
- [ ] A sprint completa possui validação end-to-end nos targets disponíveis.

## Decisões

- o ViewModel é parametrizado por `CatalogAnimeId` e criado por entrada da
  Navigation 3;
- o carregamento começa na criação do ViewModel;
- os estados são `Loading`, `Content` e `Failure`;
- retry é aceito somente após falha, evitando chamadas concorrentes iniciadas
  pela interface;
- a S7.1 mantém uma apresentação mínima; o layout definitivo pertence às
  subtasks visuais seguintes.

## Bloqueios, riscos e mudanças de escopo

Não houve bloqueio. O build nativo iOS depende de macOS e Xcode; no ambiente
Windows foi compilada somente a metadata iOS.

## Evidências

Em 2026-09-24, a S7.1 foi validada com:

| Target | Resultado |
|---|---|
| Desktop | 265 testes, 0 falhas |
| Android | 261 testes, 0 falhas; APK debug gerado |
| JavaScript | 293 testes, 0 falhas |
| WasmJS | 293 testes, 0 falhas |
| Common e iOS | metadata compilada |

O smoke test visual no Web WasmJS confirmou o carregamento pelo deep link
`#/anime/20` e a transição para o estado de conteúdo com a identidade correta.
Falha e retry foram comprovados por testes automatizados. `git diff --check`
não encontrou erros, somente avisos informativos de LF/CRLF.

## Encerramento parcial

### Entregue

A S7.1 foi aceita em 2026-09-24. A tela deixou de ser um placeholder sem
consulta: ela consome o ViewModel real e representa loading, conteúdo mínimo e
falha com retry.

### Não entregue

A apresentação completa de capa, banner, títulos, sinopse, metadados e conteúdo
complementar não pertence à S7.1.

### Débitos e limitações

- erro e retry ainda não receberam smoke test visual forçado;
- o build nativo iOS permanece pendente de macOS e Xcode;
- os avisos não bloqueantes já conhecidos de KLIB, NPM, Webpack e WasmJS
  permanecem fora do escopo.

### Próximo passo

Implementar a S7.2 — estrutura visual principal, sem iniciá-la automaticamente.
