# Sprint S6 — Busca de Animes End-to-End

> **Estado:** Em andamento
> **Registro:** contemporâneo
> **Roadmap:** [Sprint 6](../roadmap.md#sprint-6--busca-de-animes-end-to-end)

## Objetivo

Entregar a primeira funcionalidade vertical do aplicativo: pesquisar animes na
AniList, apresentar resultados e navegar com o ID AniList selecionado.

## Baseline anterior à implementação

- `AnimeRepository` oferece pesquisa paginada e detalhes por `AniListAnimeId`;
- modelos, mapeadores, paginação e falhas pertencem ao domínio;
- `SearchScreen` e `AnimeDetailsScreen` ainda são placeholders;
- `AppRoute.AnimeDetails` ainda não transporta ID;
- não existe carregador de imagens remotas para a UI Compose.

## Escopo e subtasks

| ID | Resultado | Estado |
|---|---|---|
| S6.1 | Auditoria e plano de implementação | Concluída e aceita |
| S6.2 | Estado e primeira pesquisa funcional | Concluída e aceita |
| S6.3 | Navegação de detalhes com ID AniList | Concluída e aceita |
| S6.4 | Resultados responsivos e imagens | Concluída e aceita |
| S6.5 | Paginação e recuperação incremental | Concluída e aceita (S6.5.1 a S6.5.7) |
| S6.6 | Validação End-to-End | Planejada |

## Decisões da S6.1

- criar estado assíncrono específico da busca, sem abstração genérica antecipada;
- invalidar e cancelar a consulta anterior assim que a consulta normalizada
  mudar; o debounce atrasa somente a nova requisição;
- usar `PageInfo.hasNextPage` como fonte de verdade da paginação;
- deduplicar resultados por `AniListAnimeId` e preservar itens diante de falha
  incremental;
- verificar separadamente ViewModel, resultados e posição de scroll no retorno
  dos detalhes;
- atribuir à Sprint 6 o transporte e a restauração do ID AniList;
- atribuir à Sprint 7 o carregamento e a apresentação dos detalhes pelo ID;
- manter lifecycle, integração Koin e Coil como propostas até comprovação de
  compatibilidade;
- não prometer cache HTTP sem evidência por target.

## Dependências e riscos

| Item | Estado | Condição |
|---|---|---|
| Lifecycle/ViewModel | Integrado | testes em Desktop, Android, JS e WasmJS; iOS nativo pendente |
| Integração Koin/Compose | Integrada | ViewModelStore por entrada; retorno da pesquisa validado na S6.3 |
| Coil | Integrado | Coil 3.4.0 com Ktor 3 compilou em Android, Desktop, metadata iOS, JS e WasmJS; execução iOS nativa pendente |
| Cache de imagens | Padrão do Coil | memória e disco habilitados; sem política HTTP, expiração, invalidação ou offline |
| Build nativo iOS | Pendente de ambiente | exige macOS e Xcode |

## Evidências da S6.1

- código, contratos, navegação, UI, Koin e dependências inspecionados;
- plano incremental aceito pelo usuário;
- nenhum código ou dependência alterado durante a auditoria;
- encerramento documental publicado no commit `c381217`.

## Execução e encerramento da S6.2

**Concluída e aceita em 2026-09-12**, mediante aceite explícito do usuário.
A proposta inicial de estados foi substituída pelo comportamento implementado
em [Estados da busca](../modelagem.md#estados-da-busca).

- `SearchUiState` e `SearchViewModel` em `commonMain`, com `StateFlow`, consulta
  normalizada, debounce de 800 ms e pesquisa automática a partir de três
  caracteres; consultas curtas não vazias aceitam submissão explícita.
- Primeira página de até 20 animes via `AnimeRepository`, cancelamento e proteção
  por geração/consulta contra respostas obsoletas; estados inicial, debounce,
  loading, resultados, vazio, falha e cooldown com mensagens seguras.
- Lista de títulos com fallback, limpeza do campo, ação de pesquisa do teclado
  e retry. `AppSearchBar`/`AppTextField` encaminham opções e ações de teclado;
  `AppLoading` usa descrição padrão quando recebe texto em branco.
- Cooldown local de 15 segundos após `RateLimited`, seguido de uma única
  tentativa automática. Alteração não vazia mantém o contador e atualiza a
  próxima consulta; limpar cancela a espera. Novo rate limit na tentativa
  automática termina em falha. Não há consumo de `Retry-After` nessa fronteira.
- Lifecycle 2.10.0 (`viewmodel-compose`, `runtime-compose` e
  `viewmodel-navigation3`) e `koin-compose-viewmodel` 4.2.2 em `commonMain`;
  `kotlinx-coroutines-swing` 1.10.2 em Desktop fornece o dispatcher Main.
  Versões centralizadas no catálogo, com Navigation 3 1.1.1.
- Koin registra ViewModel não singleton; a tela observa estado com lifecycle e
  resolve a instância no `ViewModelStore` da entrada do Navigation 3, decorada
  após o estado salvável. Decisão: [ADR-007](../adr/ADR-007-escopar-viewmodels-por-entrada-navigation3.md).

### Validações e alcance das evidências

Os relatórios locais preexistentes de 2026-09-12 foram inspecionados neste
encerramento, sem nova execução de build ou testes. Os nomes abaixo são as
tasks identificadas nos diretórios `composeApp/build/test-results/`; não se
reconstrói uma linha de comando histórica cujos argumentos não foram registrados.

| Testes automatizados | Testes no relatório | Falhas / erros |
|---|---:|---:|
| `:composeApp:desktopTest` | 240 | 0 / 0 |
| `:composeApp:testDebugUnitTest` | 238 | 0 / 0 |
| `:composeApp:jsBrowserTest` | 263 | 0 / 0 |
| `:composeApp:wasmJsBrowserTest` | 263 | 0 / 0 |

Cada um desses relatórios inclui 14 testes de `SearchViewModelTest`, três de
`SearchTitleTest` e um de `ViewModelModuleTest`. Cobrem normalização, debounce,
submissão, cancelamento, resposta obsoleta, sucesso, vazio, retry, cooldown e
resolução não singleton no Koin. O teste Koin não é, isoladamente, teste de
retenção/remoção da entrada real do Navigation 3.

O cooldown foi **validado por tempo virtual**, com repository controlado:
contagem, uso da consulta mais recente, cancelamento ao limpar e ausência de
ciclo após segundo `RateLimited`. Não houve teste deliberado de rate limit
contra a AniList. O ramo de consulta curta durante cooldown foi inspecionado no
código, sem caso automatizado específico.

A confirmação manual da pesquisa em **Web, Desktop e Android** integra o aceite
informado pelo usuário neste encerramento; não foi repetida aqui e não deve ser
confundida com os testes automatizados ou ampliada para imagens, paginação e
navegação de detalhes com ID. Não há registro granular adicional de dispositivos
ou passos manuais neste encerramento.

Os relatórios de testes comprovam execução nos targets indicados, não builds de
distribuição. Não foi recuperado log de build separado para atribuir comandos ou
resultados adicionais. Metadata iOS não substitui build ou execução nativa:
**iOS nativo permanece não validado**, dependente de macOS com Xcode.

No encerramento documental, `git diff --check` passou, com avisos LF/CRLF
separados de erros. Foram revisados o diff, a sintaxe dos dois diagramas Mermaid
da busca (inspeção textual, sem renderizador) e 49 links locais/âncoras dos
documentos afetados, sem destinos ausentes. A comparação SHA-256 confirmou a
preservação de 152 arquivos de código, testes, recursos e Gradle inspecionados.

### Limitações e pendências no encerramento da S6.2

- Imagens/Coil, paginação incremental e navegação de detalhes com ID não entregues.
- Preservação de ViewModel, resultados e scroll no retorno dos detalhes ainda
  depende do fluxo futuro; troca entre destinos principais remove a entrada.
- Divergência frente à intenção de respeitar `Retry-After` no contrato AniList:
  o prazo não é propagado nem utilizado; os 15 segundos são política local.
- Refinamento visual do foco no tema escuro permanece na Sprint 16.
- Encerramento apenas documental, preservando implementação, testes,
  dependências e configurações preexistentes; sem stage, commit ou push.

### Validação para publicação da S6.2

Após o encerramento documental, a invocação explícita de
`mykytadu-git-publish` autorizou stage, novo commit e push da entrega aceita.
As 24 alterações foram classificadas como pertinentes à S6.2: implementação,
recursos, testes, dependências de lifecycle e documentação. Nenhum arquivo
alheio, temporário ou sensível foi selecionado.

Tasks confirmadas com `./gradlew.bat :composeApp:tasks --all --console=plain`
(sucesso). Regressão executada com:

```powershell
.\gradlew.bat :composeApp:compileKotlinDesktop :composeApp:desktopTest :composeApp:assembleDebug :composeApp:testDebugUnitTest :composeApp:compileCommonMainKotlinMetadata :composeApp:compileIosMainKotlinMetadata :composeApp:jsBrowserTest :composeApp:wasmJsBrowserTest --rerun-tasks --console=plain
```

Resultado: **BUILD SUCCESSFUL**, 121 tasks executadas em 2 min 20 s. Testes:
Desktop 240, Android debug 238, JS 263 e WasmJS 263, sem falhas ou erros.
Compilação Desktop, APK Android debug e metadata comum/iOS concluídos.
Não foram repetidos testes manuais nem executados builds de distribuição Web
ou build nativo iOS nesta publicação.

O acesso inicial ao lock do cache Gradle foi negado pelo sandbox; a execução
foi repetida com permissão ampliada e concluída. O fetch também exigiu acesso
de escrita a `.git/FETCH_HEAD`; após a repetição, `master` e `origin/master`
estavam sincronizadas, sem commits locais pendentes antes do novo commit.

Avisos observados, sem falha de validação: resolução NPM durante configuração,
depreciação Node `url.parse()`, cópia sobreposta de recursos JS, dependência
dinâmica no bundle WasmJS, biblioteca Android empacotada sem stripping e nomes
KLIB duplicados na metadata iOS (incluindo Lifecycle, Compose e SavedState).
Esses avisos não comprovam compatibilidade nativa iOS e permanecem registrados
para acompanhamento, sem ajustes de código ou dependências nesta publicação.

## Execução e encerramento da S6.3

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `AppRoute.AnimeDetails` passou a exigir `AniListAnimeId`; a serialização da
  navegação aceita somente inteiros positivos sem criar IDs fictícios.
- Selecionar um resultado encaminha seu ID para a rota de detalhes. A tela ainda
  é um placeholder: exibe o ID e retorna à pesquisa, sem carregar detalhes da
  AniList, que permanecem no escopo da Sprint 7.
- `mykytadu://app/anime/{id}` e `#/anime/{id}` resolvem IDs decimais positivos;
  URLs inválidas caem no fallback seguro. O Web canonicaliza zeros à esquerda e
  converte o fragmento legado `#/anime-details` para a pesquisa.
- A restauração da pilha preserva o prefixo já existente. Ao retornar dos
  detalhes por botão, histórico ou restauração Web, a pesquisa preserva a
  instância do ViewModel, seus resultados e a posição de scroll.
- No WasmJS, a tela aguarda os recursos de texto antes de criar o botão de
  retorno, evitando uma ação sem rótulo durante o carregamento assíncrono.

### Validações e alcance das evidências

Tasks confirmadas com `./gradlew.bat :composeApp:tasks --all --console=plain`.
Regressão executada com:

```powershell
.\gradlew.bat :composeApp:compileKotlinDesktop :composeApp:desktopTest :composeApp:assembleDebug :composeApp:testDebugUnitTest :composeApp:compileCommonMainKotlinMetadata :composeApp:compileIosMainKotlinMetadata :composeApp:jsBrowserTest :composeApp:wasmJsBrowserTest --console=plain
```

Resultado: **BUILD SUCCESSFUL**, 121 tasks. Testes: Desktop 247, Android debug
244, JS 276 e WasmJS 276, sem falhas ou erros. A compilação Desktop, o APK
Android debug e metadata comum/iOS concluíram.

O teste Compose Desktop cobre seleção, ID, retorno por botão, restauração pelo
histórico, ViewModel, resultados, scroll e teclado. Os testes comuns e Web
cobrem serialização, deep links, caminhos inválidos, histórico, reload e
fragmentos canônicos. O smoke test manual WasmJS pesquisou `Naruto` na AniList,
abriu `#/anime/16870`, fez reload direto, voltou e avançou pelo navegador e
retornou pelo botão preservando a posição da lista. O URL `#/anime/0` foi
canonicamente redirecionado a `#/splash`.

Os avisos de configuração NPM, cópia de recursos JS, interop WasmJS, nomes KLIB
duplicados na metadata iOS e stripping de biblioteca Android não interromperam
a regressão. Build e execução nativos iOS continuam pendentes de macOS/Xcode.

## Execução e encerramento da S6.4.1

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `AnimeSummary` já fornece `coverLarge` e `coverExtraLarge`; a UI atual ainda
  usa `LazyColumn` e `AppCard` sem carregador de imagens.
- A auditoria selecionou Coil 3 com `coil-network-ktor3` como candidato para
  `commonMain`: o artefato de rede Ktor 3 é necessário ao target WasmJS e o
  projeto já usa Ktor 3.5.2.
- Coil 3.4.0 é a versão inicial de validação, por ter sido publicada com Kotlin
  2.3.10 e Compose 1.9.3, próximos de Kotlin 2.3.20 e Compose 1.10.3 usados no
  projeto. Essa proximidade não substitui a resolução e a compilação reais.
- Cache HTTP não foi habilitado nem especificado: Coil não respeita
  `Cache-Control` por padrão e qualquer comportamento de cache será registrado
  somente após observação por target.

### Validações e limites das evidências

`./gradlew.bat :composeApp:tasks --all --console=plain` concluiu com
**BUILD SUCCESSFUL** e confirmou as tasks para Android, Desktop, metadata iOS,
JS e WasmJS. A auditoria não alterou código, Gradle ou dependências; portanto,
ela não comprova a compatibilidade binária de Coil. Essa comprovação pertence à
validação incremental que começa na S6.4.2.

## Execução e encerramento da S6.4.2

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `AnimeSearchResultCard` substituiu o uso direto de `AppCard` na busca e
  preserva clique, foco e navegação existentes.
- A capa usa `coverExtraLarge` ou `coverLarge`; URL ausente ou falha mantém uma
  superfície neutra e o título continua acessível no card.
- Coil 3.4.0, `coil-compose` e `coil-network-ktor3` foram centralizados no
  catálogo e adicionados a `commonMain`.

### Validações e limites das evidências

`compileCommonMainKotlinMetadata`, `compileKotlinDesktop` e `desktopTest`
concluíram com sucesso. A compilação de regressão para Android debug, Desktop,
metadata iOS, JS e WasmJS também concluiu com **BUILD SUCCESSFUL** (59 tasks).
Os avisos de KLIB duplicada na metadata iOS e stripping Android não causaram
falha. A execução nativa iOS exige macOS/Xcode; renderização real, grade e
cache permanecem nas subtasks seguintes.

## Execução e encerramento da S6.4.3

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `SearchScreen` usa `LazyVerticalGrid` e `LazyGridState`, mantendo as chaves
  por `AniListAnimeId`, seleção, foco, teclado e restauração da posição.
- A grade usa duas colunas quando a largura disponível do conteúdo fica abaixo
  de 600dp e quatro a partir do breakpoint compartilhado. O card passou ao
  formato vertical, com capa 2:3 e título de até duas linhas.

### Validações e limites das evidências

`compileCommonMainKotlinMetadata`, `compileKotlinDesktop` e `desktopTest`
concluíram com sucesso. Android debug, metadata iOS, JS e WasmJS também
compilaram com **BUILD SUCCESSFUL** (57 tasks). O teste Desktop existente
confirmou seleção, retorno e restauração de scroll na grade. A aparência em
navegador e dispositivos permanece pendente de validação manual na S6.4.6.

## Execução e encerramento da S6.4.4

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `AsyncImage` usa o singleton padrão do Coil. As políticas padrão de memória,
  disco e rede estão habilitadas segundo a API oficial da versão integrada.
- Não foi criado `ImageLoader` próprio nem adicionada dependência de
  `Cache-Control`. Coil 3 salva respostas de rede no cache de disco por padrão,
  mas não interpreta seus headers HTTP sem configuração adicional.
- Não foram definidos tamanho ou diretório de cache, expiração, invalidação,
  fallback offline, nem qualquer promessa de persistência por target.

### Validações e limites das evidências

A configuração atual foi inspecionada: não há política de cache específica no
código e `AsyncImage` usa o loader padrão. O comportamento documentado da API
define as políticas padrão; leitura/escrita observada em navegador ou dispositivo
continua pendente de validação manual na S6.4.6. Cache persistente de catálogo e
experiência offline permanecem no escopo da Sprint 14.

## Execução e encerramento da S6.4.5

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `SearchCoverUrlTest` cobre prioridade para `coverExtraLarge`, fallback para
  `coverLarge`, remoção de espaços e ausência de URL.
- `SearchNavigationUiTest` confirma dois cards na mesma linha compacta e
  preserva seleção, teclado, retorno e restauração de scroll.

### Validações e limites das evidências

O teste direcionado Desktop passou. A regressão com `desktopTest`,
`testDebugUnitTest`, metadata iOS, `jsBrowserTest` e `wasmJsBrowserTest`
concluiu com **BUILD SUCCESSFUL** (102 tasks). As tasks oficiais de atualização
dos lockfiles JS e WasmJS foram executadas, sem alteração em lockfile rastreado.
Avisos de npm, webpack, KLIB duplicada e interop WasmJS não causaram falha.
Execução nativa iOS permanece pendente de macOS/Xcode; a validação visual Web
WasmJS foi concluída na S6.4.6.

## Execução e encerramento da S6.4.6

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- O servidor local `wasmJsBrowserDevelopmentRun` executou a aplicação WasmJS
  em `http://localhost:8080`.
- Pesquisas reais na AniList exibiram capas remotas em cards com título legível.
- A grade apresentou duas colunas a 599 px e quatro colunas em largura
  expandida. Após recarregar a 1000 px, a grade confirmou quatro colunas;
  portanto, o breakpoint de 600dp é aplicado à largura disponível do conteúdo,
  e não deve ser descrito como largura absoluta da janela.
- A seleção abriu o detalhe com o AniList ID correto; voltar restaurou a
  pesquisa e seus resultados.

### Limites das evidências

A recarga voltou a exibir capas, mas a inspeção visual não distingue leitura de
memória, disco ou rede. Assim, ela não comprova um hit de cache nem amplia a
política padrão do Coil. Não houve execução visual em Android, Desktop ou iOS;
o build e a execução nativos iOS continuam dependentes de macOS/Xcode.

## Execução e encerramento da S6.5.1

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `SearchContent.Results` passou a preservar os itens e o `PageInfo` da
  primeira resposta aceita.
- `SearchPaginationState` diferencia `Idle`, `Loading` e `Failure` para o
  carregamento incremental, sem substituir o estado de loading inicial.
- Na S6.5.1, a solicitação ainda estava limitada à página 1; o carregamento da
  próxima página foi implementado na S6.5.2.

### Validações e limites das evidências

`desktopTest` e `testDebugUnitTest` concluíram com **BUILD SUCCESSFUL** (42
tasks), sem falhas. A subtask não adicionou requisições incrementais, gatilho de
rolagem ou tratamento de retry; esses comportamentos permanecem pendentes.

## Execução e encerramento da S6.5.2

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `SearchViewModel.loadNextPage()` solicita `currentPage + 1` somente quando a
  resposta atual informa `hasNextPage`.
- O estado `Loading` impede chamadas concorrentes; a geração e a consulta atual
  impedem que respostas obsoletas alterem a tela.
- A resposta aceita é acumulada e seu `PageInfo` passa a governar a próxima
  solicitação. O retry visual continua pendente.

### Validações e limites das evidências

`desktopTest` e `testDebugUnitTest` concluíram com **BUILD SUCCESSFUL** (42
tasks), sem falhas. A grade ainda não dispara o método automaticamente e o
retry visual será tratado na próxima subtask.

## Execução e encerramento da S6.5.3

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- A mesclagem mantém a primeira ocorrência de cada `AniListAnimeId`, tanto
  entre páginas quanto dentro da página recebida.
- A ordem dos itens já exibidos permanece estável e o `PageInfo` da resposta
  aceita continua governando a próxima solicitação.

### Validações e limites das evidências

`desktopTest` e `testDebugUnitTest` concluíram com **BUILD SUCCESSFUL** (42
tasks), sem falhas. O retry visual e o disparo automático pela grade continuam
pendentes.

## Execução e encerramento da S6.5.4

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- `retryNextPage()` repete a página pendente após uma falha incremental.
- Os itens já exibidos permanecem disponíveis durante a falha e após a
  recuperação bem-sucedida.
- A nova tentativa mantém a consulta e o número da página; falhas obsoletas
  continuam impedidas pela geração da execução.

### Validações e limites das evidências

`desktopTest` e `testDebugUnitTest` concluíram com **BUILD SUCCESSFUL** (42
tasks), sem falhas. A integração do estado de falha com a grade e sua ação
visual de retry permanecem para a S6.5.5.

## Execução e encerramento da S6.5.5

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- A `LazyVerticalGrid` solicita a próxima página quando a rolagem se aproxima
  dos últimos quatro itens, respeitando `PageInfo.hasNextPage` e os guardas do
  ViewModel.
- O rodapé usa largura completa para exibir carregamento incremental ou falha
  com a ação de retry, reutilizando os componentes do Design System.
- Os textos de carregamento e erro incremental foram adicionados aos recursos
  compartilhados.

### Validações e limites das evidências

`desktopTest` e `testDebugUnitTest` concluíram com **BUILD SUCCESSFUL** (42
tasks), sem falhas. A validação visual manual permanece planejada para a
S6.5.7.

## Execução e encerramento da S6.5.6

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- O teste de UI verifica o disparo da página seguinte ao rolar próximo ao fim
  da grade.
- O mesmo fluxo verifica loading incremental, falha preservando os resultados,
  retry da página pendente e inclusão do resultado recuperado.

### Validações e limites das evidências

`desktopTest` direcionado à `SearchNavigationUiTest` e a execução combinada de
`desktopTest` com `testDebugUnitTest` concluíram com **BUILD SUCCESSFUL** (42
tasks), sem falhas. A validação visual manual permanece planejada para a
S6.5.7.

## Execução e encerramento da S6.5.7

**Concluída e aceita em 2026-09-15**, mediante aceite explícito do usuário.

- A aplicação Web WasmJS foi executada no servidor de desenvolvimento e aberta
  no Chrome.
- A busca real por “Naruto” exibiu capas remotas carregadas em grade expandida
  de quatro colunas.
- A rolagem vertical até o fim dos resultados preservou o layout e os títulos.
- Os estados de página adicional, erro e retry não foram reproduzidos com a
  resposta real usada; eles permanecem cobertos pelo teste automatizado da
  S6.5.6.

### Validações e limites das evidências

O servidor Web WasmJS iniciou em `http://localhost:8080/` e a inspeção visual
foi concluída no Chrome. A cobertura multiplataforma completa permanece na
S6.6.

## Próximo passo

S6.6 — validação End-to-End. A Sprint 6 permanece em andamento.
